#include <bits/stdc++.h>
using namespace std;
int main (){
    string s = "xxoxxxox";
    int count = 0;
    for (int i  = 1 ; i < s.length()-2;i++){
        if (s[i] == 'o' && s[i-1] == 'x' && s[i+1] == 'x'){
            count++;
        }
        if(s[0]=='x' and )
    }
}